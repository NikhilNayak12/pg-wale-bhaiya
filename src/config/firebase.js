// Firebase stub for static/demo mode
// This file intentionally avoids importing the real Firebase SDK so the
// frontend can run without any backend during the hackathon demo.

// If any part of the app still imports `auth` or `db`, they'll receive `null`.
export const auth = null;
export const db = null;

export default null;
